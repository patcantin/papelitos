<!-- SMALL PAPELITOS LOGO -->
<%= image_tag "papelitos_logo.png", class: "small_logo" %>

<% @game_users.each do |u| %>
   <p><%= u.user.name %></p>
<% end %>

   <% if @game_user == 1 %>
      <%= link_to "START", game_path(@game.id), class: 'buttons' %>
  <% end %>


